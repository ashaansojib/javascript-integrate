// click handaler
document.getElementById("sing-button").addEventListener('click', function(){
    // username field
    const getUserName = getInputFieldById("userField");
    // username full
    const getUserFullName = getInputFieldById("nameField");
    // user email
    const getUserEmail = getInputFieldById("userEmail");
    // get the password
    const getUserPassword = getInputFieldById("userPassword");
    // set error function if user provide invaid info
    if(getUserName === 'sojib' && getUserFullName === 'khan' && getUserEmail === 'sojib@bd.com' && getUserPassword === '9080'){
        console.log("the login is successful");
    }
    else{
        alert("your input value is wrong");
    }
    errorStyle("userPassword");
 })